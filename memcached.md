# Install memcached:

```bash
	sudo apt install memcached php-memcached
	sudo systemctl enable memcached
	sudo systemctl start memcached
```


# References:

[1](http://www.servermom.org/install-use-memcached-nginx-php-7-ubuntu-16-04/3670/) install memcached ubuntu